import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Primary Button Colors (Gradient)
  static const Color primaryGradientStart = Color(0xFF38BDF8);
  static const Color primaryGradientEnd = Color(0xFF06B6D4);
  
  // Secondary Button Colors
  static const Color secondaryText = Color(0xFF006194);
  
  // TextField Colors
  static const Color textPrimary = Color(0xFF131B2E);
  static const Color borderColor = Color(0xFFE2E7FF);
  
  // General Common Colors
  static const Color white = Colors.white;
  static const Color transparent = Colors.transparent;
}
